# LinuxScripts
Ubuntu container that let u run scripts inside

run this from windows to open bash inside container:
docker exec -it ubuntu-container bash

files inside script folder are hot swappable
executable.sh make all files in script executable